
  # Pedido sharing app

  This is a code bundle for Pedido sharing app. The original project is available at https://www.figma.com/design/amnJYPgySdSPxEPdxPRNkS/Pedido-sharing-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  